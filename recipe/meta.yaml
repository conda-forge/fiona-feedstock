{% set version = "1.7.2" %}

package:
  name: fiona
  version: {{ version }}

source:
  fn: fiona-{{ version }}.tar.gz
  url: https://github.com/Toblerity/Fiona/archive/{{ version }}.tar.gz
  sha256: e1b353294afad04f11f28ecda3735ecbf33537106d9d5603e3f50bba4953d7c4

build:
  number: 0
  preserve_egg_dir: True
  entry_points:
    - fio=fiona.fio.main:main_group

requirements:
  build:
    - python
    - cython
    - setuptools
    - numpy x.x
    - gdal 2.1.*
  run:
    - python
    - setuptools
    - numpy x.x
    - gdal 2.1.*
    - cligj
    - munch
    - click-plugins
    - six

test:
  source_files:
    fiona
  imports:
    - fiona
    - fiona.fio
  requires:
    - nose
    - pytest
  files:
    - test_data/test.cpg
    - test_data/test.dbf
    - test_data/test.shp
    - test_data/test.shx
  commands:
    - cd fiona/tests && nosetests --exclude test_filter_vsi --exclude test_geopackage --exclude test_write_mismatch tests  # [linux]
    - fio --help
    - conda inspect linkages -p $PREFIX fiona  # [not win]
    - conda inspect objects -p $PREFIX fiona  # [osx]

about:
  home: http://github.com/Toblerity/Fiona
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'Fiona reads and writes spatial data files.'

extra:
  recipe-maintainers:
    - ocefpaf
