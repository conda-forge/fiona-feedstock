{% set version = "1.7.3" %}

package:
  name: fiona
  version: {{ version }}

source:
  fn: fiona-{{ version }}.tar.gz
  url: https://github.com/Toblerity/Fiona/archive/{{ version }}.tar.gz
  sha256: f43198f7e6685027fd9d25bf55a0ceb0897639beb07a92f31964de24cf930598

build:
  number: 0
  preserve_egg_dir: True
  entry_points:
    - fio=fiona.fio.main:main_group

requirements:
  build:
    - python
    - cython
    - setuptools
    - numpy x.x
    - gdal 2.1.*
  run:
    - python
    - setuptools
    - numpy x.x
    - gdal 2.1.*
    - cligj
    - munch
    - click-plugins
    - six

test:
  source_files:
    - tests
  imports:
    - fiona
    - fiona.fio
  requires:
    - nose
    - pytest
  files:
    - test_data
  commands:
    - fio --help
    - conda inspect linkages -p $PREFIX fiona  # [not win]
    - conda inspect objects -p $PREFIX fiona  # [osx]

about:
  home: http://github.com/Toblerity/Fiona
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'Fiona reads and writes spatial data files.'

extra:
  recipe-maintainers:
    - ocefpaf
