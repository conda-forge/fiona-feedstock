{% set version = "1.7.12" %}

package:
  name: fiona
  version: {{ version }}

source:
  url: https://github.com/Toblerity/Fiona/archive/{{ version }}.tar.gz
  sha256: bc49a4d2434ac860de8feeff13bcc4608da4124c102aa0fff8ec3c6883153acf
  patches:
    - fix_null_gdal_22.patch

build:
  number: 0
  entry_points:
    - fio=fiona.fio.main:main_group

requirements:
  build:
    - python
    - pip
    - cython
    - numpy 1.11.*
    - gdal 2.2.*
  run:
    - python
    - setuptools
    - numpy >=1.11
    - gdal 2.2.*
    - cligj
    - munch
    - click-plugins
    - six >=1.7
    - enum34  # [py27]
    - shapely

test:
  source_files:
    - tests
  imports:
    - fiona
    - fiona.fio
  requires:
    - pytest
  files:
    - test_data
  commands:
    - fio --help
    - fio ls test_data/test.shp
    - fio info test_data/test.shp
    - conda inspect linkages -p $PREFIX $PKG_NAME  # [not win]
    - conda inspect objects -p $PREFIX $PKG_NAME  # [osx]

about:
  home: http://github.com/Toblerity/Fiona
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'Fiona reads and writes spatial data files.'

extra:
  recipe-maintainers:
    - ocefpaf
