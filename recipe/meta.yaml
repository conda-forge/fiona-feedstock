{% set version = "1.8a1" %}

package:
  name: fiona
  version: {{ version }}

source:
  url: https://github.com/Toblerity/Fiona/archive/{{ version }}.tar.gz
  sha256: 1a229bb511b0b228357556251a2c66c9a96e4321b195e2c67b996b8f93c78283
  patches:
    - fix_null_gdal_22.patch

build:
  number: 0
  entry_points:
    - fio=fiona.fio.main:main_group

requirements:
  build:
    - python
    - pip
    - cython
    - numpy 1.11.*
    - gdal 2.2.*
  run:
    - python
    - setuptools
    - numpy >=1.11
    - gdal 2.2.*
    - cligj
    - munch
    - click-plugins
    - six
    - enum34  # [py27]
    - shapely

test:
  source_files:
    - tests
  imports:
    - fiona
    - fiona.fio
  requires:
    - pytest
  files:
    - test_data
  commands:
    - fio --help
    - fio ls test_data/test.shp
    - fio info test_data/test.shp
    - conda inspect linkages -p $PREFIX $PKG_NAME  # [not win]
    - conda inspect objects -p $PREFIX $PKG_NAME  # [osx]

about:
  home: http://github.com/Toblerity/Fiona
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'Fiona reads and writes spatial data files.'

extra:
  recipe-maintainers:
    - ocefpaf
